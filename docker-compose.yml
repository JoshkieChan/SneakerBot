services:
  sneakerbot:
    build: .
    restart: on-failure
    stop_grace_period: 1s
    env_file:
      - .env
    volumes:
      - ./.env:/app/.env
      - ./config/config.json:/app/config/config.json
      - ./data:/app/data
      - ./logs:/app/logs
    logging:
      driver: "json-file"
      options:
        max-size: "10mb"
        max-file: "3"
    deploy:
      resources:
        limits:
          memory: 800M
